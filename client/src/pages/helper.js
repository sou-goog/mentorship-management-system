// client/src/pages/helper.js

console.log('--- [DEBUG] helper.js (in pages directory) IS PARSED ---');

const helperMessage = "Hello from helper in the pages directory!";

export default helperMessage;